# =============================================================================
# Emotion Recognition RAG System - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
#
# NOTE: Most configuration is in config.py with sensible defaults.
# This .env file is for:
#   1. Secrets (API keys, passwords)
#   2. Environment-specific overrides (prod vs dev settings)
#
# Only uncomment variables you need to override!
# =============================================================================

# -----------------------------------------------------------------------------
# Vector Database (Qdrant)
# -----------------------------------------------------------------------------
# Configured in config.py - uncomment only to override:
# QDRANT_HOST=localhost
# QDRANT_PORT=6333
# QDRANT_COLLECTION_NAME=emotion_recognition_papers

# -----------------------------------------------------------------------------
# Embedding Models
# -----------------------------------------------------------------------------
# Configured in config.py - uncomment only to override:
# Dense embeddings (semantic search)
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Sparse embeddings (keyword search)
# SPARSE_MODEL=Qdrant/bm25
# USE_HYBRID_SEARCH=true

# Embedding processing
# EMBEDDING_BATCH_SIZE=16  # Reduce to 8 or 4 if GPU memory issues

# -----------------------------------------------------------------------------
# LLM Configuration
# -----------------------------------------------------------------------------
# Note: LLM_MODEL is configured in config.py (default: gpt-4-turbo-preview)
# Only override these if you need different settings from the defaults

# Choose your LLM provider: "openai" or "vllm"
# LLM_PROVIDER=openai

# Uncomment to override the default model from config.py
# LLM_MODEL=gpt-4o

# Uncomment to override generation parameters
# LLM_TEMPERATURE=0.1
# LLM_MAX_TOKENS=2000

# -----------------------------------------------------------------------------
# OpenAI Configuration (REQUIRED if using OpenAI)
# -----------------------------------------------------------------------------
# ⚠️ REQUIRED: Set your OpenAI API key here
OPENAI_API_KEY=your-openai-api-key-here

# ⚠️ REQUIRED: OpenAI API base URL (default: https://api.openai.com/v1)
OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Custom OpenAI endpoint (for Azure OpenAI, etc.)
# OPENAI_BASE_URL=https://your-azure-endpoint.openai.azure.com/

# -----------------------------------------------------------------------------
# vLLM Configuration (if LLM_PROVIDER=vllm)
# -----------------------------------------------------------------------------
# Configured in config.py - uncomment only to override:
# vLLM server URL (OpenAI-compatible endpoint)
# VLLM_BASE_URL=http://localhost:8000/v1
# VLLM_API_KEY=EMPTY

# -----------------------------------------------------------------------------
# Chunking Parameters
# -----------------------------------------------------------------------------
# Configured in config.py - uncomment only to override:
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=200

# -----------------------------------------------------------------------------
# Retrieval Parameters
# -----------------------------------------------------------------------------
# Configured in config.py - uncomment only to override:
# DEFAULT_TOP_K=8

# -----------------------------------------------------------------------------
# API Server Configuration
# -----------------------------------------------------------------------------
# Configured in config.py - uncomment only to override:
# API_HOST=0.0.0.0
# API_PORT=8000

